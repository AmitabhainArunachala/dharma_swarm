{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the DHARMA COMMAND desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
